# Publish cli-monarch to PyPI via Trusted Publisher (OIDC) when manually dispatched.
# Requires a PyPI trusted publisher for lukeslp/cli-monarch + GitHub Environment "pypi".

name: Publish to PyPI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pypi-publish:
    name: Upload release to PyPI
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/cli-monarch/
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          python -m pip install build twine
      - name: Build package
        run: python -m build
      - name: Check metadata
        run: python -m twine check dist/*
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cli-monarch-dist
          path: dist/*
      - name: Publish package to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
